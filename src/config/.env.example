# Example env for local development

# Example env for local development
# Fill these values locally (do NOT commit secrets). Copy this file to src/.env
AGENT_MODEL_DEPLOYMENT_NAME=
PROJECT_ENDPOINT=

# (Optional) Service principal values — required for CI / non-interactive auth
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=

AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP_NAME=
MCP_SERVER_URL=
AGENT_TEMPERATURE=0.2
